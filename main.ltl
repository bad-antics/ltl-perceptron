// ltl-perceptron/main.ltl
//
// Single-layer perceptron with margin analysis.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-perceptron v0.1.0")
    io::println("Single-layer perceptron with margin analysis.")
    0
}
